# .github/workflows/radar-scrape.yml
# M1 Radar de Subexecução — cron semanal Portal da Transparência para AL/SE/PE/BA.
# Trigger manual disponível em Actions → "Radar — scrape Portal Transparência" → Run workflow.
# Requer secret PORTAL_TRANSPARENCIA_API_KEY (cadastrar em
# https://api.portaldatransparencia.gov.br/swagger-ui/index.html).

name: Radar — scrape Portal Transparência

on:
  schedule:
    # Segunda-feira 06:00 UTC = 03:00 BRT (madrugada Brasil)
    - cron: '0 6 * * 1'
  workflow_dispatch:
    inputs:
      ufs:
        description: 'UFs (separadas por vírgula)'
        required: false
        default: 'AL,SE,PE,BA'

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 60
    env:
      SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
      PORTAL_TRANSPARENCIA_API_KEY: ${{ secrets.PORTAL_TRANSPARENCIA_API_KEY }}
      RADAR_UFS: ${{ github.event.inputs.ufs || 'AL,SE,PE,BA' }}
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r scraper/requirements.txt

      - name: Run Portal Transparência scraper
        run: python -m scraper.run --source portal_transparencia --ufs "$RADAR_UFS"
